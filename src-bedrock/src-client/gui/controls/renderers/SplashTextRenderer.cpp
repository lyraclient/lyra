#include "SplashTextRenderer.hpp"
#include <spdlog/spdlog.h>

void SplashTextRenderer::render_hk(void* ctx, void* clientInstance, void* owner, int pass, void* renderAABB) {
    spdlog::debug("inside the render hook");
}